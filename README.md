# UU AMF Bridge Arch repository

This branch contains the generated Pacman repository published through GitHub
Pages. Source code lives on the `main` branch.

The repository is transported over HTTPS but is not currently GPG-signed. Its
configuration therefore uses `SigLevel = Optional TrustAll` explicitly.
